---
description: Abandon a change and clean up dependencies
argument-hint: [change-name]
---

Abandon a change that is no longer needed.

**Input**: $ARGUMENTS (optional change name, or inferred from context)

Deletes the change directory and cleans up dependency references in other changes.

Use the drop skill to execute this workflow.
