---
description: Initialize delta-spec in a repository
---

Initialize delta-spec in this repository.

Creates the `specs/` directory structure and optionally generates initial specs from existing code.

Use the init skill to execute this workflow.
