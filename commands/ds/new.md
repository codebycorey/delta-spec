---
description: Start a new delta-spec change with a proposal
argument-hint: <change-name>
---

Start a new delta-spec change.

**Input**: $ARGUMENTS (change name in kebab-case, or description of what to build)

Creates `specs/.delta/<name>/proposal.md` and works interactively to define the problem and scope.

Use the new skill to execute this workflow.
