---
description: Create design and delta specs for a change
argument-hint: [change-name]
---

Create a design document and delta specs for a change.

**Input**: $ARGUMENTS (optional change name, or inferred from context)

Explores the codebase, creates `design.md`, and generates delta specs in `specs/.delta/<name>/specs/`.

Use the plan skill to execute this workflow.
