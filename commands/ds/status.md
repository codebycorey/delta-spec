---
description: Show active delta-spec changes
---

Show status of all active changes.

Lists changes in `specs/.delta/`, showing which artifacts exist, dependencies status, and recommended next steps.

Use the status skill to execute this workflow.
