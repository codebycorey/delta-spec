---
description: Create implementation tasks for a change
argument-hint: [change-name]
---

Create actionable implementation tasks for a change.

**Input**: $ARGUMENTS (optional change name, or inferred from context)

Reads the proposal, design, and delta specs, then creates tasks using Claude Code's native TaskCreate tool.

Use the tasks skill to execute this workflow.
